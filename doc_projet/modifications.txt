- **Build Vercel (CategoryFilters) :**
    - **Problème résolu :** Erreur de rendu statique ("Event handlers cannot be passed to Client Component props") lors du déploiement Vercel.
    - **Solution :** Ajout de la directive `"use client";` dans `src/components/ui/CategoryFilters.tsx` car ce composant gère des interactions utilisateur (onClick, onValueChange) et doit être rendu côté client.
- **Correctifs & Optimisations Techniques (CategoryMarquee) :**
    - **Conformité Tailwind CSS v4 :** Remplacement des classes `bg-gradient-to-*` par `bg-linear-to-*` pour corriger les erreurs de compilation.
    - **Pause au Survol :** Ajout de la classe `group-hover:[animation-play-state:paused]` pour arrêter le défilement lors de l'interaction utilisateur.

## 16. Création des Interfaces "Direct" et "Programmes"
- **Page Direct (`src/app/direct/page.tsx`) :**
    - Implémentation d'une interface dédiée au live streaming.
    - Intégration du composant `VideoPlayer` en mode "théâtre" (large et sombre).
    - Ajout d'une barre latérale affichant le programme du jour avec indicateur de statut (En cours, À venir).
    - Intégration de boutons d'action (Partager, Chat) et d'appel aux dons.
- **Page Programmes (`src/app/programmes/page.tsx`) :**
    - Création d'une bibliothèque de contenus avec filtrage par catégorie (`CategoryFilters`).
    - Implémentation d'une grille responsive de cartes (`ProgramCard`) avec effets de survol (zoom, overlay dégradé, soulignement bleu).
    - Ajout d'une modale de détails (`Modal`) permettant de visionner le programme et voir les informations associées sans quitter la page.
- **Composants UI :**
    - `ProgramCard.tsx` : Carte réutilisable pour l'affichage des programmes en grille.
    - `modal.tsx` : Composant de fenêtre modale animé avec `framer-motion` et `createPortal` pour une superposition propre.
